<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="000-create-schema" author="fund-transfer-gateway"
               runAlways="false" failOnError="false">

        <!-- create schema if not exists -->
        <sql>
            CREATE SCHEMA IF NOT EXISTS gateway_service;
        </sql>

        <!-- grant full access to app user -->
        <sql>
            GRANT ALL PRIVILEGES ON SCHEMA gateway_service TO "fund-transfer";
        </sql>

        <!-- auto grant future tables to app user -->
        <sql>
            ALTER DEFAULT PRIVILEGES IN SCHEMA gateway_service
            GRANT ALL PRIVILEGES ON TABLES TO "fund-transfer";
        </sql>

        <!-- auto grant future sequences (BIGSERIAL) to app user -->
        <sql>
            ALTER DEFAULT PRIVILEGES IN SCHEMA gateway_service
            GRANT ALL PRIVILEGES ON SEQUENCES TO "fund-transfer";
        </sql>

        <!-- set default search path for app user -->
        <sql>
            ALTER ROLE "fund-transfer" SET search_path TO gateway_service, public;
        </sql>

        <rollback>
            DROP SCHEMA IF EXISTS gateway_service CASCADE;
        </rollback>

    </changeSet>

</databaseChangeLog>